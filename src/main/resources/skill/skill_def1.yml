__option__:
  name: 't'
  variables:
    cooldown: 0
    mp: math meta skill level * 2.8
    damage: math meta skill level * 1.2 * lazy base
    upgradePoint: math meta skill level * 2
    base: 10
action: |-
  
  selector g0 to "-@range 10" 
  for target in selector g0 list then {
    attack flag 被击血印 they &target &target
    flag 被击血印 to 0 timeout 20 they &target
    profile flag &target to 0 timeout 20 
  }
    

  

  
